#pragma once

namespace content
{
    namespace description {
        inline constexpr char plane[] = "Plane (%d, %d) (%d, %d)";
        inline constexpr char train[] = "Train (%d, %d) (%d)";
        inline constexpr char truck[] = "Truck (%d, %d) (%d, (%d/%d))";
    }
}
